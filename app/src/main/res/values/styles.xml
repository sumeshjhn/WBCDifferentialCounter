<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/ActionBarStyle</item>

        <!-- Support library compat -->
        <item name="actionBarStyle">@style/ActionBarStyle</item>
        <item name="colorPrimary">@color/red_colorPrimary</item>
        <item name="colorPrimaryDark">@color/red_colorSecondary</item>
    </style>

    <style name="ActionBarStyle" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:background">@color/red_colorPrimary</item>

        <!-- Support library compat -->
        <item name="background">@color/red_colorPrimary</item>
    </style>
</resources>
